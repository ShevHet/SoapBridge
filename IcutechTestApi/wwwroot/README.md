# Production Frontend Build

Production-оптимизированная версия фронтенда для Icutech Test API.

## Быстрый старт

```bash
# Установка зависимостей
npm install

# Сборка production версии
npm run build:prod

# Или через Make
make build
```

## Структура файлов

```
wwwroot/
├── index.html              # Development версия
├── index-prod.html         # Production версия (с inline CSS)
├── styles.css              # Исходный CSS
├── styles.min.css          # Минифицированный CSS (генерируется)
├── app.js                  # Исходный JS
├── app.min.js              # Минифицированный JS (генерируется)
├── build.js                # Скрипт сборки
├── test-pagespeed.js       # Скрипт тестирования производительности
├── package.json            # NPM зависимости и скрипты
├── Makefile                # Make команды
├── BUILD_INSTRUCTIONS.md   # Подробные инструкции по сборке
├── OPTIMIZATION_GUIDE.md   # Руководство по оптимизации
└── TEST_OUTPUT_EXAMPLE.md  # Примеры вывода тестов
```

## Оптимизации

### ✅ Реализовано

- [x] Минификация CSS и JS
- [x] Inline критический CSS
- [x] Асинхронная загрузка CSS (preload)
- [x] Defer загрузка JavaScript
- [x] Мета-теги (viewport, description, theme-color)
- [x] Заголовки кэширования (Cache-Control)
- [x] Gzip/Brotli сжатие
- [x] Lighthouse/PageSpeed тесты

### 📋 Оптимизации изображений

См. `OPTIMIZATION_GUIDE.md` для:
- Конвертации в WebP/AVIF
- Batch обработки
- Responsive images
- Lazy loading

## Команды

### NPM скрипты

```bash
npm run build          # Создание production HTML
npm run minify:css     # Минификация CSS
npm run minify:js      # Минификация JS
npm run build:prod     # Полная сборка
npm run test:lighthouse # Lighthouse тест
npm run test:pagespeed  # PageSpeed тест с проверкой порогов
```

### Make команды

```bash
make install           # Установка зависимостей
make build             # Полная сборка
make minify-css        # Минификация CSS
make minify-js         # Минификация JS
make test              # Сборка + тесты
make test-lighthouse   # Только Lighthouse
make clean             # Очистка build файлов
make all               # Установка + сборка + тесты
```

## Тестирование

### Требования

- Node.js 16+
- Lighthouse CLI: `npm install -g lighthouse`

### Запуск тестов

```bash
# Убедитесь, что API запущен
dotnet run --project ../IcutechTestApi.csproj

# В другом терминале
npm run test:pagespeed
```

### Пороги производительности

- **Mobile**: >= 100
- **Desktop**: >= 90
- **Render-blocking**: 0 ресурсов

Тест завершится с ошибкой, если пороги не достигнуты.

## Использование

### Development

Используйте `index.html` для разработки.

### Production

Используйте `index-prod.html` в production. Он включает:
- Inline критический CSS
- Минифицированные ресурсы
- Оптимизированную загрузку

### Развертывание

1. Соберите production версию:
   ```bash
   npm run build:prod
   ```

2. Скопируйте файлы на сервер:
   - `index-prod.html` → `index.html`
   - `styles.min.css`
   - `app.min.js`

3. Убедитесь, что настроены:
   - Заголовки кэширования (см. `BUILD_INSTRUCTIONS.md`)
   - Gzip/Brotli сжатие (настроено в `Program.cs`)

## Дополнительная информация

- **BUILD_INSTRUCTIONS.md** - Подробные инструкции по сборке
- **OPTIMIZATION_GUIDE.md** - Руководство по оптимизации изображений и ресурсов
- **TEST_OUTPUT_EXAMPLE.md** - Примеры вывода тестов производительности

